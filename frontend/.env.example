# Base URL of the IIMA Marketplace API
NEXT_PUBLIC_API_URL=http://localhost:4000/api/v1

# Google OAuth Web Client ID (public — safe to expose to the browser).
# Must match GOOGLE_CLIENT_ID on the backend. Leave blank in dev for mock sign-in.
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
